<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.sms.mapper.SmsFlashPromotionProductRelationMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.macro.mall.sms.model.SmsFlashPromotionProductRelation">
        <id column="id" property="id" />
        <result column="flash_promotion_id" property="flashPromotionId" />
        <result column="flash_promotion_session_id" property="flashPromotionSessionId" />
        <result column="product_id" property="productId" />
        <result column="flash_promotion_price" property="flashPromotionPrice" />
        <result column="flash_promotion_count" property="flashPromotionCount" />
        <result column="flash_promotion_limit" property="flashPromotionLimit" />
        <result column="sort" property="sort" />
    </resultMap>

    <resultMap id="flashProductMap" type="com.macro.mall.sms.dto.SmsFlashPromotionProduct" extends="com.macro.mall.sms.mapper.SmsFlashPromotionProductRelationMapper.BaseResultMap">
        <association property="product" resultMap="com.macro.mall.pms.mapper.PmsProductMapper.BaseResultMap" columnPrefix="p_"/>
    </resultMap>
    <select id="getList" resultMap="flashProductMap">
        SELECT
            r.id,
            r.flash_promotion_price,
            r.flash_promotion_count,
            r.flash_promotion_limit,
            r.flash_promotion_id,
            r.flash_promotion_session_id,
            r.product_id,
            r.sort,
            p.id p_id,
            p.`name` p_name,
            p.product_sn p_product_sn,
            p.price p_price,
            p.stock p_stock
        FROM
            sms_flash_promotion_product_relation r
            LEFT JOIN pms_product p ON r.product_id = p.id
        WHERE
            r.flash_promotion_id = #{flashPromotionId}
            AND r.flash_promotion_session_id = #{flashPromotionSessionId}
        ORDER BY r.sort DESC
    </select>

    <resultMap id="flashPromotionProduct" type="com.macro.mall.sms.dto.FlashPromotionProduct"
               extends="com.macro.mall.pms.mapper.PmsProductMapper.BaseResultMap">
        <result column="flash_promotion_price" property="flashPromotionPrice"/>
        <result column="flash_promotion_count" property="flashPromotionCount"/>
        <result column="flash_promotion_limit" property="flashPromotionLimit"/>
    </resultMap>
    <select id="getFlashProductList" resultMap="flashPromotionProduct">
        SELECT
            pr.flash_promotion_price,
            pr.flash_promotion_count,
            pr.flash_promotion_limit,
            p.*
        FROM
            sms_flash_promotion_product_relation pr
            LEFT JOIN pms_product p ON pr.product_id = p.id
        WHERE
            pr.flash_promotion_id = #{flashPromotionId}
            AND pr.flash_promotion_session_id = #{sessionId}
    </select>

</mapper>
